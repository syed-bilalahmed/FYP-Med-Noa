// Basic Interactivity
document.addEventListener('DOMContentLoaded', () => {
    // Add active class handling if using AJAX nav, 
    // but we use PHP routing so the server handles 'active' class mostly.

    // Maybe toggle sidebar on mobile?
});
